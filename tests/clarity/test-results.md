# clarity 验证结果与使用限制

本页汇总与当前技能有关的验证证据。[测试用例](test-prompts.json)保存 70 个场景的输入与预期；来源和案例版本见 [来源记录](../../docs/clarity/sources.md)。

## 代码职责与内联规则调整

本轮明确代码表达与实现政策的边界，区分首次编写、获准的破坏性迭代、保行为重构和功能修改；同步小 helper 的内联规则、局部概念命名及提取建议。README、主文件和代理默认提示同步更新，不改已安装副本或其他技能。

新增七个场景，覆盖无复用 helper 内联、明确复用计划、局部中间量、获准破坏性改名、开发流程边界、局部改名和状态角色命名。70 是用例定义总数，不是行为通过数；下方历史模型结果不覆盖本轮修改。

已完成的直接检查：

- 70 个用例 ID 唯一，输入、类型与预期字段有效；JSON 可解析。
- 主文件 frontmatter 与代理 YAML 可解析，技能名及 `$clarity` 提示正确。
- 八份运行时 Markdown 的本地链接文件目标存在、代码围栏配对；没有引用本轮移除的两个标题锚点，既有报错信息锚点仍在。
- 新增内联前后的两段 Python 示例可执行；空列表、两个名称、缺少 `items`、缺少 `name` 四个场景的结果或异常类型与参数一致。不代表全部输入等价或人类理解效果。
- 已删除完整流程优先、强制收益说明、先记录再运行检查及删除派生状态的旧指令；`git diff --check` 通过。

### 独立上下文复核与场景回答

只读 reviewer 复核确认本轮约定已落地，未发现当前文本的实质内部冲突。与另行提供的 `code-implement` 四份文件比较，在本次任务边界和高层数据流的解释下，未发现要求执行真实相反动作的规则；“允许提取”不等于“必须提取”，明确复用计划也不等于想象中的未来用途。这是静态比较，不代表两个技能共载时的自动执行已验证。

审查者实际生成七个新增场景和四个既有回归场景的回答。维护者读取完整原始回答并按预期复核，与审查者自评一致：**11 pass、0 partial、0 fail**。

| 场景组 | 实际覆盖与结果 |
|---|---|
| 七项新增 | `inline-unreused-helper`、`helper-reuse-plan`、`inline-with-local-concepts`、`authorized-breaking-rename`、`readability-not-development-process`、`local-rename-proportional`、`state-roles-not-redesign`：全部通过 |
| 四项回归 | `code-async-semantics`、`naming-contract-compatibility`、`code-coherent-function`、`code-only-validation`：全部通过 |

工作流 ID：`5522b11d-9e45-499d-bfdc-15013bc4202c`；审查运行 ID：`a8fabb7e-74a5-4e79-ae5c-77891ae2d28f`。原始回答保存在该运行的 `clarity-final-review.md` 产物中，不复制全文到仓库。审查期间运行时文件及用例保持不变；复核返回后仅补充本页记录。

审查使用独立上下文，但预读了技能和 expected，因此不是盲测、自动路由或真人效果评估。11 是本轮实际回答数，不是全量 70 用例通过数；除上节四个示例输入外，本轮没有执行这些模型回答对应的代码测试。

## 历史模型结论：命名指导增量

- 命名指导增量后的 fresh-context Luna 审查完整读取 14 份文件：根 README、九份运行时文件、三份维护说明与完整用例定义；未发现实质规则冲突或需要修复的结构问题。
- 审查同时检查技能自身的文件职责、路径命名、标题层级和规则自用，没有只检查新增段落。
- 本次实际生成八个新增命名场景及两个既有回归场景的回答，审查者与维护者复核均为 **10 pass、0 partial、0 fail**。

本次模型审查运行 ID：`cf4dc2de-53bb-4c08-8faa-aa0b30f9000a`。输入文件在审查期间保持不变，返回后只更新本页验证记录；原始回答保留在执行会话中，不复制逐轮报告到仓库。

这些回答与 expected 处于同一个预读技能的模型上下文中，不是盲测、自动路由测试或真人效果评估。63 是该次审查时的用例定义总数，不是该轮全量行为通过数。

## 历史结构检查：命名指导增量

检查对象为 `clarity/`、`docs/clarity/`、`tests/clarity/` 和根 README：

- 九份运行时文件、七份参考，主文件路由与实际名称一致；运行时不依赖来源或测试记录。
- Frontmatter 名称与目录匹配；代理默认提示包含 `$clarity`；README 有安装和维护入口。
- 63 个用例 ID 唯一，均含输入、类型和预期；旧参考文件名已同步替换。
- 本地链接目标及使用到的标题锚点有效；Markdown 围栏配对，运行时无行尾空白。
- 目录整理保留既有来源的采用边界、案例版本与署名；删除临时来源清单、原始报告及重复状态汇总。
- `git diff --check` 通过。

这些检查不等于安装测试或真实运行环境的自动路由验证。

## 模型回答覆盖与偏差

### 命名增量后的十项回答

| 场景 | 结果与核对要点 |
|---|---|
| `doc-path-grouping` | pass：三页按安装、备份和配置命名，入口链接明确，未创建空分类 |
| `doc-reference-navigation` | pass：参考保留按对象查阅，版本层保留兼容意义 |
| `title-section-purpose` | pass：标题覆盖有效期与续期，通用前提不再置于例子下 |
| `paragraph-title-restraint` | pass：保留小说标题和连续叙事，不把草稿职责标签写进成品 |
| `naming-role-scope` | pass：预留动作、重试策略和秒单位可见，清楚的局部索引不强改 |
| `naming-contract-compatibility` | pass：只改局部名称，保留公共签名、JSON 键、单位换算及键缺失行为 |
| `naming-language-context` | pass：保留 Go 调用语境，不移植 .NET 前缀或机械加长名称 |
| `rename-document-contract` | pass：保留机器文件名、已发布地址和锚点，迁移检查不冒充已执行 |
| `paper-abstract-scope` | pass：标题与摘要保留语境条件，未加入禁止否定句等提示防错内容 |
| `code-only-validation` | pass：单价参数改名，函数与数量参数不变，只交付代码 |

维护者另执行了本次两段实际 Python 回答：单位换算覆盖六个数值输入及缺键时抛 `KeyError`、不调用等待函数的边界；单价命名覆盖四个乘法输入。检查通过，不代表所有输入等价或真实等待行为已测。

### 增量前的覆盖与保留偏差

原文件名版本的全文件审查运行 ID 为 `8930676b-041a-4135-ae15-ab58c2dd5e91`。该次十二项回答由审查者自评全部通过，维护者复核为 **11 pass、1 partial、0 fail**。以下保留该次结果，不以本次通过覆盖历史偏差。

| 场景 | 结果 |
|---|---|
| `compatible-not-opposed`：日志与测试的互补职责 | pass |
| `claim-task-match`：帮助理解与保证系统可靠性不同 | pass |
| `retain-grounded-correction`：保留有依据的日期更正 | pass |
| `retain-literary-contrast`：保留文学重心转移 | pass |
| `retain-consequential-limit`：保留样本与因果限制 | pass |
| `paper-abstract-scope`：论文摘要的证据与正文范围 | partial |
| `paper-structure-proportional`：CCC、回顾与方法的适用边界 | pass |
| `c4-levels`：系统、容器与部署副本分开 | pass |
| `execplan-scope`：评估协议适用性并完成局部改写 | pass |
| `api-docstring-contract`：工具可见性及调用契约 | pass |
| `plan-subject-not-editorial`：保留实施状态与理由 | pass |
| `code-only-validation`：只交付授权的代码改写 | pass |

`paper-abstract-scope` 的摘要末句为：“不能据此提出全面禁用否定句的结论。”给定研究考察语境与合宜度，没有讨论禁用政策；回答把提示中的防错要求带进了正文。现有正文与语境规则已覆盖这个问题，不为单次执行偏差追加同义规则，也未重写原回答后宣称原样通过。

这些回答出自预读 skill、维护材料与 expected 的同一审查上下文。当时定义了 55 个用例，后续命名增量扩为 63 个；这些数字均不是该轮全量行为通过数，不同轮次的回答不合并为一次独立试验。

两次语义审查之间的目录整理另经只读复核：参考职责、标题层级和证据压缩获得认可；根 README 已补明 `survey-md` 没有独立维护材料。该次仅调整组织、命名与两处标题归属，没有执行新的模型用例。

## 代码案例的直接核对

| 检查 | 结果与范围 |
|---|---|
| Node.js：异步包装与直接返回同步抛错 | 两项断言通过：async 返回 rejected Promise，普通包装可同步抛错 |
| Python：价格公式命名前后 | 12,288 个有限数值组合严格相等，另有四个边界场景符合预期；不覆盖自定义数值类型、非有限值或有副作用读取 |
| CPython `compiler_mod`、Go `protoAtLeast` | 分别按逐字、忽略空白方式与来源固定提交匹配；未构建原项目 |
| Go `IndexRune` 两个调用 | 本地 `go1.27.1-X:nodwarf5 linux/amd64`、`GOTOOLCHAIN=local` 返回字节偏移 3 和未找到 -1；不冒充来源 go1.23.0 全套测试 |
| Python docstring 与普通注释 | `inspect.getdoc` 能读取前者，不能读取后者；示例函数结果相同，未运行 Sphinx |
| Rust 诊断节选 | 两段文本与固定版本输出一致；未运行 Rust 编译器 |

上述实验针对相应案例编写时的版本；本次命名增量未改变这些既有代码围栏，也未重新运行这组历史实验。本次新执行的两段模型代码单独记录在上节，不混入历史结果；有限测试不能推出所有输入等价或可读性效果。

## 尚未验证

- 自动选择技能、按需加载参考及跨会话稳定性。
- 全量 70 用例的独立行为试跑、无 skill 对照与跨模型比较。
- 真人阅读速度、理解正确率、满意度及母语者审校。
- 全部引用项目的构建、生产行为或性能。

后续改动应选择与风险有关的用例。静态一致、某次模型回答符合预期和人类效果分别记录，不能相互替代。
